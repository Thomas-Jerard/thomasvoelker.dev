# thomasvoelker.dev

Personal site for Thomas Voelker — founder of Orilo.

This repo is the source of truth. Grok Build sandboxes get wiped; this does not.

Restore: clone, `npm install`, `npm run dev` (port 8080).
